# Lab 5 - Starter


- expose: https://mantuetr.github.io/Lab5_Starter/expose.html

- explore: https://mantuetr.github.io/Lab5_Starter/explore.html

1. Would you use a unit test to test the “message” feature of a messaging application? Why or why not? For this question, assume the “message” feature allows a user to write and send a message to another user.

No. Because unit tests are more for testing individual component such as input validation. But, the message feature
including type, send, received, edit,...
2. Would you use a unit test to test the “max message length” feature of a messaging application? Why or why not? For this question, assume the “max message length” feature prevents the user from typing more than 80 characters.

Yes. Because the unit tests are for testing individual component so just to check the maximum length of message is just
one part, and does not involve on other parts.
